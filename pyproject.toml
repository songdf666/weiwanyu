[project]
name = "euphemism-reproduction-code"
version = "0.1.0"
description = "Organized reproduction code, experiment templates, and preprocessing framework for euphemism and semantic-change studies."
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py39"
